const no=document.getElementById("no"),yes=document.getElementById("yes"),hint=document.getElementById("hint"),card=document.getElementById("card"),success=document.getElementById("success");let tries=0;function flee(e){if(e)e.preventDefault();tries++;const r=card.getBoundingClientRect(),b=no.getBoundingClientRect(),pad=22,maxX=r.width-b.width-pad,maxY=r.height-b.height-pad,x=pad+Math.random()*Math.max(1,maxX-pad),y=220+Math.random()*Math.max(1,maxY-220);no.style.position="absolute";no.style.left=x+"px";no.style.top=y+"px";no.style.zIndex=20;hint.textContent=["ih, quase... 😭","não vale tentar apertar!","foi por pouco 👀","esse botão não quer colaborar...","você realmente tentou o NÃO? 😭","ele fugiu de novo ♡"][Math.min(tries-1,5)]}no.addEventListener("mouseenter",flee);no.addEventListener("pointerdown",flee);yes.addEventListener("click",()=>{success.classList.add("show");for(let i=0;i<12;i++)setTimeout(heart,i*100)});function heart(){const x=document.createElement("div");x.className="float";x.textContent=Math.random()>.35?"♡":"♥";x.style.left=Math.random()*100+"vw";x.style.bottom="-30px";x.style.fontSize=16+Math.random()*25+"px";document.body.appendChild(x);setTimeout(()=>x.remove(),8000)}